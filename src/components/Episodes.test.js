import React from 'react';
import { render } from '@testing-library/react';
import Episodes from './Episodes';

test('Renders the main image from API', () => {
  const {} = render(
    <Episodes episodes={{}}/>
  );
}) 