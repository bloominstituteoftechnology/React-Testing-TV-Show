import React from "react";
import { render } from "@testing-library/react";
import Episodes from "./Episodes";

test("Episodes component renders correctly", () => {
//     // const { getByText, queryAllByTestId } = render(<Episodes />);

//     const { queryAllByTestId, rerender } = render(
//         <Episodes seasons={[]} />
//       );

//       expect(queryAllByTestId("test-episodes")).toStrictEqual([]);
//       expect(queryAllByTestId("test-episodes")).toHaveLength(0);

//       rerender(<Episodes  seasons={seasons} />);

//       expect(queryAllByTestId("test-episodes")).toHaveLength(10);

});
